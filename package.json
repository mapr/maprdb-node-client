{
  "name": "mapr-node-bindings",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test:build": "npm run build && ntsc -p ./test/tsconfig.server.json",
    "test:run": "node ./test/dist/server",
    "test:check": "node ./test/dist/client",
    "clean": "rm -rf ./dist/* && rm -rf ./test/dist/*",
    "prepublish": "npm run build",
    "proto": "mkdir -p ./dist/protos && pbjs -t static-module -w commonjs ./protos/maprdb-server.proto | pbts -o ./dist/protos/index.d.ts -",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "ntsc -p tsconfig.release.json",
    "watch": "ntsc -w",
    "lint": "tslint -c tslint.json -p tsconfig.json",
    "lint-fix": "tslint -c tslint.json -p tsconfig.json --fix"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "grpc": "^1.8.4",
    "grpc-health-check": "^1.6.6",
    "ntypescript": "^1.201706190042.1",
    "tslint": "^5.9.1"
  },
  "devDependencies": {
    "@types/jest": "^22.1.1",
    "@types/node": "^9.4.0",
    "jest": "^22.1.4",
    "protobufjs": "^6.8.5",
    "ts-node": "^4.1.0",
    "tslint-microsoft-contrib": "^5.0.2",
    "tsutils": "^2.21.0",
    "typescript": "^2.7.1"
  }
}
