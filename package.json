{
  "name": "mapr-node-bindings",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/index.js",
  "scripts": {
    "clean": "rm -rf ./dist",
    "preinstall": "npm run clean",
    "postinstall": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "ntsc -p tsconfig.release.json",
    "watch": "ntsc -w",
    "lint": "tslint -c tslint.json -p tsconfig.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ntypescript": "^1.201706190042.1",
    "tslint": "^5.9.1"
  },
  "devDependencies": {
    "@types/jest": "^22.1.1",
    "@types/node": "^9.4.0",
    "jest": "^22.1.4",
    "ts-node": "^4.1.0",
    "tslint-microsoft-contrib": "^5.0.2",
    "tsutils": "^2.21.0",
    "typescript": "^2.7.1"
  }
}
