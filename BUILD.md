### JS library publishing steps

* Publish to custom repository

``
npm publish --registry $NPM_REPO_URL
``

### If you want to install package from custom npm repository
``
npm install node-maprdb --registry $NPM_REPO_URL
``